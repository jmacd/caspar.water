# Cloud (Linode) terraform variables
#
# Copy to terraform.tfvars and fill in secrets.
# This file is gitignored — credentials stay local.
#
# The cloud host serves the static site (rsync'd from watershop) via
# caddy.  It does not run any watertown instance and so does not need
# R2 credentials.

# Linode API token
li_token = ""
